# arabic-itsm-classification — Python dependencies
# Python 3.10+

# Core ML
# ⚠️  WARNING: Do NOT install torch with bare `pip install torch`.
#    That installs the CPU-only wheel (torch+cpu) which silently disables GPU.
#    Install PyTorch separately FIRST with the correct CUDA index URL:
#
#    Step 1: Update NVIDIA driver to ≥550 from https://www.nvidia.com/Download/
#    Step 2: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#    Step 3: Verify: python -c "import torch; assert torch.cuda.is_available()"
#
#    See ADR-005 in CLAUDE.md and docs/experiment_analysis.md §7 for full context.
# torch>=2.1.0   ← intentionally commented out; install via CUDA wheel above
transformers>=4.38.0
datasets>=2.18.0
accelerate>=0.27.0
evaluate>=0.4.1

# Arabic NLP
arabert>=1.0.1          # Arabic preprocessing utilities (farasa-based)
pyarabic>=0.6.15        # Arabic text utilities (diacritics, normalization)

# Data science
pandas>=2.1.0
numpy>=1.26.0
scikit-learn>=1.4.0

# Visualization
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.18.0

# Notebooks
jupyter>=1.0.0
ipywidgets>=8.1.0
tqdm>=4.66.0

# Experiment tracking
mlflow>=2.10.0

# Configuration
pyyaml>=6.0.1
python-dotenv>=1.0.0

# Export / serving
onnx>=1.15.0
onnxruntime>=1.17.0

# Utilities
tokenizers>=0.15.0
safetensors>=0.4.2
